# Triple 2 Eye Protection Windows 2.0.11

Release date: 2026-07-06

## Artifact

- `Triple2EyeProtection-Setup-2.0.11.exe`
- SHA256: `28A9ABFE3B886D34CE747BA5713D3C0BCA48E72DD038FD974FCF6ACDE79BF9D2`

## Changes

- Resized the red integrated `延时` control to the same `140x40` footprint as `开始休息` and `休息好了`.
- Kept the editable minute value inside the delay button, preserving the one-time per-popup delay behavior.
- Changed `已用眼` to use the same red color family as the delay button.
- Bumped Windows app and installer version to `2.0.11`.

## Notes

- The installer keeps the same application identity, so it can install over the previous Windows version.
- Older installers remain archived in their existing `releases/windows/*` folders.
